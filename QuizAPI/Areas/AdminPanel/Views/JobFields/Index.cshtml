@model IEnumerable<QuizAPI.Models.JobField>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.JobFieldName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.JobFieldName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.JobFieldId }) |
            @Html.ActionLink("Details", "Details", new { id=item.JobFieldId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.JobFieldId })
        </td>
    </tr>
}

</table>
